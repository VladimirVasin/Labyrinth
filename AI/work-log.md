# Work Log

## 2026-05-26

- Changed ore generation so the mineable cave closest to the dungeon entrance/exit is always forced to `Iron`; ore diversity still preserves at least one gold deposit when there are two or more deposits by never converting that protected nearest iron cave back to gold.
- Added hero footwear equipment: every knight now starts with `Сандалии` in a dedicated `Обувь` inventory slot (`0%` speed bonus), Forge services sell current-level boots (`Кожаные сапоги` +10%, `Сапоги следопыта` +20%, `Сапоги-скороходы` +30%) for personal gold, hero auto-shopping upgrades footwear only at the entrance, and movement applies the footwear speed multiplier before the final fortified-cell speed bonus.
- Fixed mine-worker and mine-cart carried-resource visuals by creating their child parts in local space instead of treating local offsets as world positions; worker timber is also reset to its hand/back offset whenever carrying state changes, so logs stay attached to the miner model. Rebalanced regular mobs upward to Rat `12/4/0`, Goblin `24/7/2`, Orc `44/10/4` HP/ATK/ARM, kept mini-boss/boss level-1 bases independent from regular mob stats, applied an `x2` HP/ATK/ARM multiplier to every mob rank on dungeon level 2+, and synced Mob HUD reward text with the combat reward table.
- Fixed building MicroHUD level/service display and completed the forge equipment ladder. Every clickable building MicroHUD now shows the current building level supplied by `BaseDevelopment`, service rows can display a small level badge, and Forge MicroHUD shows the current-level gear: level 1 sells `Стальной меч`/`Кольчуга`, level 2 sells `Рыцарский меч`/`Бригантина`, and level 3 sells `Мастерский клинок`/`Латный доспех`. Hero auto-purchase now matches the forge level. Split mine visibility helpers and mob species-selection helpers into partial files to bring all runtime `.cs` files back under the 1000-line limit.
- Reworked mine construction workers into a strict shuttle flow: up to 5 miners spawn from the Miners Guild, walk only on completed roads to the castle, take wood there, enter the labyrinth through the entrance, move only across already fortified route cells plus the current target cell, build for 2 seconds, then return to the castle for the next task. Added route validation and split worker dispatch into `MineConstructionController.Workers.cs`.
- Locked dark respawn to `MobRank.Regular` and added a defensive post-create rank check so bosses and mini-bosses cannot reappear from darkness. Mine construction routes now append a 3x3 cave-footprint reinforcement pass before final mine construction, so miners reinforce every cell around the mine site and return to the cave center before building the mine.
- Fixed lower-level entry placement: level 2 now anchors its `UpStairs` at the same grid coordinate as the level-1 opened `DownStairs`, connects that anchor to the generated lower maze if needed, and transfers heroes there instead of the generated edge entrance. Added a clickable close button to the boss-victory HUD.
- Added direct map selection for heroes: `HeroView` now has a click collider and controller reference, and clicking a knight selects that hero and opens the hero HUD. Added a shared map-cell selection marker that outlines the selected hero cell in yellow or selected mob/boss cell in red.

## 2026-05-22

- Added local AI memory folder for the Labyrinth project.
- Added base HUD hero creation.
- Added hero exploration system: the hero starts at the entrance near the base, walks only through walkable cells, remembers cells after visiting them, uses memory for DFS/backtracking, and does not receive the exit coordinate as a target.
- Added `Assets/Models/Characters/Knight/` as the drop folder for a knight character model.
- Improved camera controls: stronger wheel zoom, faster mouse/keyboard pan, plus `+`/`-` zoom and `Shift`+wheel pan.
- Enlarged the main menu panel and controls.
- Added `TimeScaleController` with `1`/`2`/`3` hotkeys for `x1`/`x2`/`x3` and a top-right time indicator.
- Added default resources `Food=50` and `Gold=50` plus a top HUD bar showing `Пища` and `Золото`.
- Moved the base HUD to the bottom-left corner and enlarged its panel, text, and buttons.
- Fixed main menu vertical fit on shorter Game View sizes by reducing excess spacing and keeping the start button inside the panel.
- Moved the time indicator to the true top-right corner, disabled the duplicate screen-space exit label, and added IMGUI scroll/drag handling as a fallback for camera zoom/pan.
- Added Escape return-to-main-menu flow: clears the current maze/hero, disables camera interaction, resets time scale to x1, and shows the start menu for regeneration.
- Added base farms: the base HUD can build farms, each farm produces +1 food per in-game second, and new map generation resets resources/farms to defaults.
- Replaced the simple base cube with a larger primitive medieval castle and replaced the hero capsule with a primitive knight that has a procedural walking animation.
- Farm construction now places a physical primitive farm on a random free cell outside the maze near the castle, with at least 1 empty cell of spacing between buildings.
- Added a left-side hero icon, hero selection, a hero HUD panel, and a selection marker on the map.
- Reworked the primitive knight into a more readable model with helmet, visor, cape, shield, sword, pivoted limbs, and improved walking animation.
- Added a basic hero inventory with weapon, armor, and one empty slot, displayed in the hero HUD.
- Cleaned up the hero HUD by removing the duplicate exploration summary line and tightening the panel layout so inventory slots fit.
- Restyled the hero HUD and castle HUD into structured dark panels with section headers, compact stat rows, framed inventory/status rows, and fixed bottom buttons.
- Removed the map information section from the castle HUD and added hero combat stats: HP, Attack Points, and Armor Points.
- Added wandering neutral orcs, clickable right-side orc HUD, and turn-based hero-vs-orc combat with adjacent positioning, hero-first attacks, attack animation, and floating damage numbers.
- Increased the farm placement exclusion around the castle footprint, added a 25 gold farm cost, and added floating `+1` food numbers above farms on production ticks.
- Added personal hero gold, displayed in the hero HUD, and awarded 10-20 gold after defeating an orc.
- Added hero stamina: the knight starts at 10/10, spends stamina only when entering new cells, returns to the castle entrance for free through remembered cells at 0 stamina, and restores there.
- Added multi-hero creation from the castle HUD, left-side hero icons for each knight, selected-hero HUD display, independent per-hero stats/inventory/resources, and shared explored-cell memory across heroes.
- Added per-hero visibility with a 2-cell line-of-sight radius, wall blocking where the first wall remains visible, selected-hero visibility highlighting, HUD visible-cell count, and F1/F2 switching between schematic and lighting modes.
- Reworked F2 lighting mode to hide real cell renderers instead of drawing fog cubes over the maze.
- Updated F2 visibility so orcs outside visible cells are hidden/non-clickable and buildings reveal a 1-cell radius around themselves.
- Made F2 unlit space fully black by switching the camera background and ambient light to black, and hid shared memory markers outside the currently lit cells.
- Added a generated physical Unity Terrain with TerrainCollider under each maze; it is recreated per map, cleared on return to main menu, and hidden visually in F2 so darkness stays black.
- Fixed F2 lighting to combine visibility from every living hero and create a separate point light for each knight instead of lighting only the selected hero.
- Added lit ground tiles for F2 building courtyards so spaces between the castle and farms stay illuminated while the surrounding terrain remains black.
- Reworked the top resource HUD into separate food/gold items with drawn icons and replaced cramped hero list buttons with wider knight cards, drawn knight icons, status text, and small HP/stamina bars.
- Fixed F1 visibility switching: schematic markers now use combined visibility from every living hero, and all F2 point lights are disabled immediately when returning to schematic mode.
- Enlarged the castle/base HUD and simplified it to a buildings-only list, removing the hero section from that window.
- Changed knight default HP to 15/15 and updated hero status/HUD HP display to show current and maximum values.
- Enlarged the primitive castle and farm visuals, moved the castle footprint farther outside the maze, and added farm placement clearance so enlarged buildings keep spacing and do not visually overlap maze cells.
- Added hero death handling with a fallen knight pose, per-hero XP, +1 XP for discovering a new shared-memory cell, +10 XP for defeating an orc, floating XP reward text, and XP display in the hero HUD.
- Added per-hero levels starting at 1, level-up checks on XP gain, level 2 at 15 XP, scaled cumulative thresholds using `15 * (level - 1) * level / 2`, and level display in the hero HUD.
- Increased the world cell size to `1.35` and scaled maze heights, buildings, heroes, orcs, visibility markers, memory markers, and floating numbers from `CellSize` while preserving grid-based generation and movement logic.
- Reworked the main menu into a wider readable layout with larger text/buttons, added manual width/height input for custom maze sizes, and centralized size normalization in `MazeGenerationSettings`.
- Restored the castle HUD hero creation button without bringing back the hero list, and changed orc spawning to spread mobs across the maze by maximizing distance between initial spawn points.
- Increased maze cell scale from `1.35` to `1.65` and moved the initial camera framing closer so cells read larger on screen instead of being fully offset by auto-fit zoom.
- Decoupled model scale from `CellSize`: heroes, orcs, castle, farms, entrance/exit decorations, and floating numbers keep the previous `1.35` visual unit while maze cells remain spaced at `1.65`.
- Changed Escape behavior from immediate return-to-main-menu to a pause menu toggle: Escape opens the menu and pauses time, repeated Escape closes it and restores the previous speed, while starting from the menu regenerates the map.
- Enlarged the left hero cards from compact icon buttons into wider readable cards so the knight icon, hero name, status, HP bar, and stamina bar fit without clipping.
- Doubled castle and farm visual scale with a separate building unit, expanded logical occupied footprints to castle radius 3 / farm radius 2 cells, increased farm placement spacing/maze clearance, and expanded building lighting to cover the larger footprint plus 1 cell.
- Limited F2 fog to the maze grid: terrain stays visible outside the labyrinth, hidden inner maze cells stay dark, and outer border wall cells remain visible as the maze contour.
- Improved the runtime terrain visuals with a procedural earthy texture, dry grass/moss/stone variation, subtle low relief, and kept the F2 maze fog cover above the new ground surface.
- Removed the separate geometric base pad under the castle so the castle stands directly on the terrain.
- Recolored the runtime terrain into a green meadow style with grass, clover patches, rare exposed earth, stones, and small flower specks.
- Added clickable `BuildingView` data to castle, farms, and hero houses, plus a compact building MicroHUD shown on building clicks.
- Creating a hero now first places a physical `Дом героя` near the castle using the same outside-maze footprint and spacing rules as other buildings; F2 building lighting now includes hero houses.
- Removed the old generated lit-ground tile layer between buildings in F2, so the castle, farms, and hero houses now sit directly on the meadow terrain without extra courtyard tiles.
- Removed the labyrinth exit concept from runtime code and spec: generation no longer creates an exit cell/marker, validation checks entrance/connectivity, and hero exploration no longer finishes on a special cell.
- Added a boss as the new win condition: one enlarged aggressive red orc variant spawns in a far reachable maze area, has stronger stats/rewards, and defeating it shows the victory HUD.
- Refactored the orc-specific runtime into a generic mob system: `MobManager`, `MobController`, `MobModel`, `MobView`, `MobHudView`, `MobSpecies`, and `MobRank` now cover ordinary mobs and the boss while orc remains the first species.
- Moved the castle base position farther outside the maze so the entrance marker remains at the dungeon edge while the castle footprint is separated from it by a small terrain gap.
- Increased regular mob density: `MobManager` now spawns roughly one ordinary mob per 38 spawn candidates, clamped to 3-18, and spreads them away from each other and the boss with a soft map-size-based minimum distance.
- Added goblins as a second regular mob species: smaller green visuals, weaker stats, lower rewards, HUD naming, and zone-weighted spawning so goblins are more common closer to the entrance while orcs are more common deeper in the maze.
- Added seed-based `3x3` caves to maze generation: maps from `15x15` upward carve open Path rooms after DFS, with count based on map area and spacing from the entrance/other caves.
- Removed ready-size buttons from the main menu and made manual size input the only path, defaulting to `100 x 25` so generation normalizes to `101 x 25`.
- Changed cave placement to choose one cave doorway from available external Path contacts, seal the extra contacts, validate full maze connectivity after the carve, and record `CaveInfo.Center` plus the single internal `EntrancePosition`.
- Added centralized `[Labyrinth]` Unity Debug logging for generation summaries, cave entrances, mob spawn counts, hero creation, farm building, combat rewards/defeats, pause flow, and boss-victory completion.
- Fixed cave generation for wide custom maps by allowing candidates on all valid internal centers instead of only existing corridor cells; debug logging now reports cave placement candidates, placed count, and rejection reasons.
- Added a mandatory central `6x6` room that splits the maze into two sections with exactly one entrance and one exit; validation now checks the bottleneck, caves cannot break the central passage, and the boss spawns only in the section beyond the room.
- Fixed extra central-room contacts by making DFS carving validate the intermediate cell of each 2-cell carve step, so the one-door room side cannot be pierced by the in-between tile.
- Added locked central-room doors and a central-room key: doors start closed/non-walkable, the key spawns in a first-half cave or fallback first-half cell, heroes can pick it into the empty inventory slot, remember locked doors, return to them, and open them when adjacent.
- Added hero stamina scaling: each level-up increases `MaxStamina` by 1 and grants the same +1 to current stamina immediately.
- Added cave chests to every regular cave except the key cave: each chest grants 10-20 personal hero gold once, opens immediately when a hero enters the cave, remembers all 3x3 cave cells with XP for newly remembered cells, keeps an opened visual, shows reward text, and plays a generated reward chime.
- Changed default hero equipment to `Ржавый меч` and `Обычная одежда`, with inventory hover info showing `+0 к атаке` and `0% снижения урона`.
- Rebalanced large-map scaling: caves now use `round(width * height / 220)` capped at 48, and regular mobs keep the 1-per-38-candidates density but scale up to 120 with softer spacing for 100x100-scale maps.
- Repositioned hero inventory hover tooltips directly beside the hovered slot and added a small gold connector arrow from the slot to the tooltip.
- Added a 5 in-game-second corpse visibility window for defeated heroes: fallen knights keep revealing cells and F2 lights briefly, then their model disappears and their personal visibility is cleared.
- Added base ambience: farms and hero houses progressively build narrow dirt roads to the castle, and completed farm roads periodically spawn small carts traveling toward the castle.
- Added an empty `Зелья` inventory slot to every knight while keeping the generic empty slot available for key pickups.
- Fixed base ambience placement rules: buildings must fit within the physical terrain bounds, and decorative roads now route only outside the maze while avoiding other building footprints.
- Added a default maximum hero cap of 5, blocked hero creation at the cap, and displayed current/max heroes in the top HUD beside food and gold.
- Offset the default castle position slightly above the labyrinth entrance and added an immediate decorative road from the castle to the entrance.
- Added the Alchemist Shop: it can be built once from the castle HUD, appears as a clickable physical building with MicroHUD/road/visibility support, and lets wounded heroes automatically spend 5 personal gold on a health potion that restores 5 HP while the payment goes into the base treasury.
- Fixed two UI/ambience regressions: widened the left hero cards and enlarged the hero HUD so `Рыцарь`/inventory slots fit, and tightened building placement spacing so roads have an actual free cell between building footprints instead of being skipped as unsafe routes.
- Changed health potion automation so heroes buy potions in advance when they have personal gold, keep up to 3 potions stocked, and still drink them automatically when missing at least 5 HP.
- Added seed-based extra maze connections after DFS and before cave placement, opening a map-size-scaled number of internal walls within each half of the labyrinth to create loops and alternate routes while preserving the central-room bottleneck.
- Extended cave chest loot: chests can now roll gold, a tier-2 `Стальной меч`, or tier-2 `Кольчуга`; heroes equip upgrades into weapon/armor slots, Attack/Armor Points are computed from base stats plus equipment bonuses, duplicates pay 5 personal gold, and chest rewards show floating text on open.
- Rebalanced combat around lower base hero stats: knights now start at 10 HP, 10 Attack Points, and 0 Armor Points; goblins/orcs/boss were retuned to 14/3/1, 22/4/2, and 50/5/3 HP/Attack/Armor while keeping real flat armor damage reduction.
- Changed farm food logistics: farms now store food locally up to 10, dispatch a cart only when a full batch is ready, and base `Food` increases only when the cart physically reaches the castle.
- Added the Tavern building: it can be built once from the castle HUD, appears as a clickable physical building with MicroHUD/road/visibility support, converts 10 base Food into a `Паёк` sold to heroes for 10 personal gold, and sends payment to the treasury; health potions now also cost 10 gold, inventory consumables are split into separate health-potion and ration stacks with drawn HUD icons, and rations restore 10 stamina automatically.
- Removed per-tick `+1` floating food numbers from farms; food accumulation is now quiet, while batch delivery feedback from farm carts remains visible at the castle.
- Reduced `CityAmbienceController` to only simple moving city walkers: all primitive town props from the previous ambience pass were removed, while walkers still rebuild from the current building list and move between safe outside-maze anchors near buildings.
- Limited automatic hero shopping to the labyrinth entrance cell: heroes may still use carried potions/rations anywhere, but they only buy or restock consumables when their current position equals `EntrancePosition`.
- Lengthened chest opening animation from `0.38s` to `2s` so cave chest lids open more visibly while rewards still trigger immediately.
- Smoothed hero movement visuals: `HeroView` now interpolates each grid step over a short eased duration and rotates toward the movement direction gradually instead of crossing a cell with a fast linear jump.
- Fixed city walkers crossing the labyrinth: walkers now build BFS paths through adjacent safe terrain cells outside `MazeGrid` and follow those waypoints instead of moving directly between distant anchors.
- Added build costs for service buildings: Alchemist Shop now costs 50 treasury gold and Tavern costs 40 treasury gold, with castle HUD statuses showing construction costs and build buttons disabled until the treasury can afford them.
- Added regular mob respawn: after kills, `MobManager` periodically rolls a small chance to restore one regular goblin/orc up to the original density, selecting only walkable unoccupied cells that are outside the current combined lit area plus a 1-cell dark padding.
- Added the Forge building: it costs 60 treasury gold, appears as a clickable physical building with MicroHUD/road/visibility support, and lets heroes at the entrance automatically buy tier-2 `Стальной меч` / `Кольчуга` for 35 personal gold each with payment going to the treasury.
- Removed defeated heroes from the active hero list and HUD immediately while keeping them in a separate corpse-visibility list until their 5 in-game-second light window expires; active/max hero counts now ignore fallen heroes.
- Added a 25 treasury gold creation cost for each hero: the castle HUD button/status now shows the price, creation is disabled without enough gold, and successful hero creation spends the gold before spawning the knight and hero house.
- Made castle HUD construction prices explicit for every buildable building: farm, alchemist shop, tavern, forge, and hero houses now show `постройка N зол.` in their status rows, and building buttons include the same gold price.
- Added gold ingots as maze collectibles: seed-based ingots spawn on valid walkable cells away from static objects and initial mobs, knights can carry one in the generic empty inventory slot, return it to the entrance for +20 treasury gold and +5 XP, and drop carried ingots back into the maze on death.
- Extended hero level-up scaling so each gained level now adds +1 Max HP and +1 current HP in addition to the existing +1 Max Stamina/current Stamina.
- Fixed farm build button availability and balance: the castle HUD now disables `Построить ферму` without 25 treasury gold, farm food production ticks once every 2 seconds per farm, and gold ingot density increased to roughly one ingot per 250 cells with a 5-60 clamp.
- Added `Assets/Audio/Music` for future music drops and `Assets/Audio/Sfx` for optional external sound effects; implemented `GameAudioController` with procedural, quiet SFX for builds, hero creation, purchases, consumables, pickups, ingot delivery, doors, chests, farm delivery, combat, level-ups, defeat, and victory.
- Connected dropped music tracks: after successful world generation, `GameAudioController` now loads audio clips from `Assets/Audio/Music` in the Editor (and `Resources/Music` for build-compatible fallback), plays them in shuffled random order, and waits 5 real seconds between tracks; music stops when returning to the main menu or starting a new generation.
- Added `Assets/Art/Menu` as the drop folder for main-menu artwork and kept in-game HUD elements hidden in menu states; time-scale hotkeys now also ignore input while the time indicator is hidden.
- Connected main-menu presentation assets: `MainMenuUI` now draws the first/`Menu` texture from `Assets/Art/Menu` as a full-screen background, positions the menu control panel in the top-right corner, and `GameAudioController` plays a separate looping `Menu` music clip in the main menu while excluding it from the in-game random playlist.
- Moved the main-menu control panel from the top-right to the top-left corner while keeping the full-screen menu background and separate menu music behavior.
- Reworked music loading to force-refresh Editor audio assets and reread the menu/world music catalog whenever music starts, so replaced MP3 files are reimported without using stale cached clips; lowered shared music volume from `0.28` to `0.16`.
- Added a defensive recovery for Play Mode script/asset reloads: if `GameBootstrap` ever reaches `Playing` with no generated maze, it logs the invalid state and returns to the main menu instead of throwing a null reference while updating hero consumables.
- Changed music timing so menu music plays in the main menu, while regular in-game music stays silent after world generation until the first hero is successfully created; the delayed in-game playlist resets on new generation or return to menu.
- Made the labyrinth entrance cell always visible in lighting/F2 mode and added a separate near-entrance extra ingot spawn pass: regular ingot density remains unchanged, while an additional seed-based batch appears within a short BFS distance from the entrance and scales with map size.
- Added the `Дерево` resource to the wallet and top HUD, plus the `Лагерь лесорубов` building: it costs 25 gold, is buildable from the castle HUD, renders as a physical clickable camp, gets ambience roads/walkers/visibility like other buildings, stores wood up to 10, and sends a cart to deliver wood to the castle before the treasury resource increases.
- Fixed decorative roads for service buildings by moving generated road endpoints one cell outside building/castle footprints, so roads from buildings like the Alchemist Shop are not hidden under models or blocked by footprint checks.
- Improved non-farm building readability: added detail props for Hero Houses, Alchemist Shops, Taverns, Forges, and Lumberjack Camps so each has a distinct silhouette and profession markers instead of the same box/roof profile.
- Added peasant-hut taxation: every two constructed buildings can auto-place a clickable hut near the base, huts slowly accumulate up to 10 gold, and a castle tax collector walks to ready huts, carries collected gold back, then deposits it into the treasury; dead knights now remove their hero house from the world/base ambience.
- Rescaled hero level thresholds to a gentler cumulative curve: level 2 remains 15 XP, then thresholds become 35, 60, 90, 125... with the next-level cost growing by 5 XP each level instead of the old steep triangular scaling.
- Made world music track switching use Unity DSP time and realtime pause deadlines instead of relying only on `AudioSource.isPlaying`, so the next shuffled track advances after clip length + 5 real seconds even if the Editor delays the playing-state flag until focus changes.
- Added mixed construction costs through a shared `BuildingCost`: farms cost 25 gold/5 wood, the first lumberjack camp costs 25 gold while later camps cost 25 gold/10 wood, hero houses cost 25 gold/10 food, and service buildings require wood; castle HUD buttons/statuses and build blocking now use required treasury resources atomically.
- Slightly increased decorative city walker visuals by scaling the primitive peasant model up while keeping their outside-maze pathing and movement logic unchanged.
- Added manual dungeon fortification: the castle HUD opens a cell-marking mode instead of an auto checkbox, selected remembered walkable cells queue worker jobs, queued cells get markers, workers spend wood to add plank floors and spaced wall torches, torch light contributes to F1/F2 visibility, and mob respawn remains blocked in lit torch areas.
- Improved dungeon fortification construction mode with a live hover marker under the cursor, showing valid cells in blue and invalid cells in red while selection mode is active.
- Made base roads and tax collection routes more aggressive around buildings: roads now connect from buildings directly to the castle through the owner/castle footprints, and tax collectors path from castle center to hut center while still avoiding the maze and unrelated building footprints.
- Added physical `debug.log` output for `GameDebugLog` in the project root and expanded base-tax diagnostics with peasant-hut registration, tax-ready, route length, return fallback, and more detailed ambient-road endpoint logs.
- Reworked the castle HUD into a tabbed control panel: `Здания`, `Герои`, and `Подземелье` now show larger action cards with icons, short statuses, separate price labels, and compact action buttons instead of one dense mixed table.
- Reworked the knight HUD into a cleaner character panel: removed position/steps/memory/visibility rows, added a header with status and XP progress, larger HP/stamina bars, gold/level chips, combat stat cards, and a two-column inventory grid with existing hover tooltips.
- Reworked the remaining HUD surfaces to match the newer card/chip style: top resources are separate chips, the time indicator uses the same compact visual language, mob HUDs use headers/HP bars/combat cards, building MicroHUDs show object cards without debug coordinates, the victory panel is more event-like, and main/pause menu panels use the shared dark/gold treatment.
- Added more branching to maze generation: extra connection density now scales higher with map area, placement prioritizes junction-producing loops, and every generated maze attempts to create a guaranteed first branch immediately after the entrance before broader loop carving.
- Split the branch/loop post-processing into `MazeBranchCarver` so `MazeGenerator.cs` stays comfortably below the 1000-line file limit while keeping maze branching logic isolated.

## 2026-05-23

- Increased default starting resources to 50 food, 75 gold, and 5 wood so the initial treasury can afford the first farm (`25 зол., 5 дер.`), first lumberjack camp (`25 зол.`), and first hero (`25 зол., 10 пищи`) in any practical opening order.
- Reworked `MainMenuUI` into a clearer card-based HUD: the top-left menu now has a stronger title block, separate cards for map size, seed, and launch summary, larger readable fields/buttons, and matching pause-menu styling.
- Fixed the redesigned main menu cards rendering empty by replacing nested GUILayout areas inside card rectangles with direct IMGUI drawing for labels, fields, and buttons.
- Polished the main menu HUD readability by making the main panel/cards more opaque and adding a little more vertical spacing between setup cards.
- Fixed central-door coordination across multiple knights: known closed doors now live in shared `HeroMemory`, key pickup logs include the hero number and known-door count, knights with the key route to shared known doors, and frontier exhaustion falls back to door opening, entrance return, or remembered-cell patrol instead of `Stuck`.
- Reworked map knowledge back to per-hero memory and added the Cartographer House: knights now keep personal explored cells/known doors, sync their knowledge with a common cartographer map only when returning to the entrance after the building exists, and newly created/returning knights can use that shared map.
- Added `Дом картографа` as a buildable/clickable base building with cost `50 зол., 15 дер.`, physical primitive model, roads/visibility/peasant-hut participation, and castle HUD support in the dungeon tab.
- Added bottom-right minimap and expanded `M` map view; the map shows either the cartographer common map or the selected hero's personal map, while F2 now keeps explored-but-not-currently-visible cells under a gray fog-of-war layer and still hides live mobs outside current visibility.
- Added the dungeon descent MVP: second-half caves now contain a locked down-stairs hatch, bosses award a `Ключ спуска` instead of ending the run, heroes can remember/open the locked stairs with that key, and opening it unlocks dungeon level 2.
- Added level rendering/switching support: level 1 keeps the base/terrain economy, level 2 renders as a pure dungeon without outside terrain/buildings, active heroes transfer to the selected level start, and a compact level-switch HUD appears after the descent is unlocked.
- Added stair cell types/visuals/minimap colors plus per-level cartographer memory so lower-level exploration can be tracked separately from the first-level shared map.
- Rebalanced in-game audio: world music volume is now lower than menu music, SFX are louder, and additional procedural SFX variants cover footsteps, keys, gold ingots, chest rewards, wood delivery, tax collection/deposit, fortification, torches, forge upgrades, stairs, and level switching.
- Added decorative ore deposits to small caves: each cave rolls iron or gold ore, places 2-4 low non-blocking ore visuals away from cave entrances/center objects, stores them in `MazeGenerationResult.OreDeposits`, and logs total/iron/gold placement counts.
- Fixed three visibility/audio regressions: ore deposits now register their child renderers after creation so F2 hides them in darkness, minimap/large map/gray remembered fog only appear after the Cartographer House is built, and world music pauses on Escape-menu or window focus loss then resumes the same track without advancing the playlist timer.
- Added a runtime audio mixer layer with separate Music/SFX/UI buses, lowered in-game music again, added light dungeon reverb/filter effects, and connected procedural UI sounds for menu open/close, HUD open/close, clicks, tabs, confirmations, map expansion, hero selection, level buttons, and major HUD panels.
- Expanded the physical terrain padding from 12 to 24 cells and changed base-building placement to search outward to the actual terrain bounds, so buildings prefer the castle area but can fall back to free cells around the whole labyrinth perimeter when the base side is full.
- Improved base readability: new games now start with the camera focused and zoomed near the castle/entrance, every BuildingView creates a readable world-space label, and selected buildings show a gold footprint outline while their MicroHUD is open.
- Added wall knowledge to exploration/cartography: heroes now remember visible wall cells separately from walkable route memory, Cartographer House sync transfers those walls into the common map, F2 treats them as explored under gray fog-of-war, and the minimap/large map marks them as known only after cartographer unlock.
- Added Rats as a weak frequent mob type: rats use separate small quadruped visuals, have 6 HP / 2 attack / 0 armor, reward 5-9 personal gold and 3 XP, spawn only in the first labyrinth section before the central room, and dark respawn now weights species by strength (Rat 55%, Goblin 30%, Orc 15%) with stronger mobs biased farther from the entrance.
- Added personal exploration pay for knights: every newly remembered walkable cell now grants the discovering hero +1 personal gold alongside +1 XP, while remembered walls and cartographer-shared knowledge remain unrewarded.
- Added the Infirmary as a buildable service building: it costs 45 gold/15 wood, gets a physical clickable model with roads/visibility/city participation, and heals knights only at the labyrinth entrance by spending 5 treasury food per restored HP.
- Reworked world HUD interactions: castle clicks now open only the large castle HUD, base/object panels close on outside clicks, mob/boss HUDs show compact reward/combat data on the right, and chests/gold ingots/ore cells have clickable Object MicroHUDs with status/effect text.
- Added the Chapel as a buildable hero-service building: it costs 55 gold/20 wood, renders as a clickable primitive chapel with roads/visibility/city/tax participation, and lets knights at the labyrinth entrance spend personal gold on exactly one expedition blessing while payments go to the treasury.
- Added 10 Chapel blessings with per-hero state and HUD display: Last Breath, Pilgrim Light, Silent Step, Hearty Path, Strong Bones, True Hand, Vow of Return, Golden Vow, Cartographer Mercy, and Dark Hunter affect combat, visibility, respawn blocking, rations, HP, return speed, rewards, and cartographer sync.
- Strengthened city ambience so every built building spawns its own profession-themed walkers: guards, farmers, lumberjacks, squires, villagers, alchemists, tavern workers, smiths, healers, cartographers, and chapel acolytes now use distinct colors/props while staying on safe outside-terrain paths.
- Improved F2 fog-of-war readability for remembered walls: known wall/door cells now use a separate dark raised silhouette with a lighter top cap instead of the same flat gray material as remembered floors.
- Changed the default labyrinth visibility mode to F2 lighting/fog-of-war on every new game start, while keeping F1/F2 hotkeys for manual switching during play.
- Made city ambience incremental on new construction: a newly built building now adds only its own road and profession walkers without destroying existing roads, carts, or walkers; empty starts no longer spawn castle-side walkers before the first player building exists.
- Added mini-bosses as a separate `MobRank.MiniBoss`: each generated central room now gets one stationary upgraded Rat/Goblin/Orc variant with boosted stats, larger aggressive visuals, HUD/reward support, and regular mob spawning excludes the central room.
- Generalized boss visuals to the same ranked-upgrade model path: the main boss now seed-selects Rat/Goblin/Orc as its visual base, applies boss-scale aggressive materials/details over that base, and keeps boss stats at least as strong as the previous 50/5/3 profile.
- Fixed far zoom terrain whitening by keeping the physical Terrain for collision but drawing the meadow as a separate procedural mesh material that does not use Terrain basemap LOD, and reduced the maximum camera zoom-out from `largestSide * 0.74` to `largestSide * 0.5`.

## 2026-05-25

- Added blessing hover descriptions to the knight HUD: hovering the active blessing card now shows the blessing name/effect using the existing Chapel blessing catalog.
- Added `BuildingServiceCatalog` and service lists to building MicroHUDs: resource/service/seller buildings now have a toggleable list with prices and hover descriptions, including the Chapel blessing list.
- Increased early goblin presence by adding a separate first-half goblin spawn layer after rats while keeping regular spread selection for the rest of the labyrinth.
- Made the central-room mini-boss mandatory: entering the central room with the mini-boss alive forces combat, and the exit-side central door stays sealed until that mini-boss is defeated.
- Removed the separate blue remembered-cell overlay after Cartographer House unlocks; cartographer knowledge now remains visible through minimap/fog-of-war only.
- Made dark-cell mob respawn more aggressive by shortening checks to 1.75 seconds, raising chance to 72%, increasing candidate scans, and allowing the respawn target to exceed initial regular mobs by about 45%.
- Changed the central room exit door behavior so defeating the mini-boss automatically opens the second door instead of merely unsealing it for a later key interaction.
- Made hero/building/object tooltip and description boxes grow with wrapped text so hover descriptions do not clip longer lines.
- Replaced the castle HUD fortification action with mining setup: added a buildable `Гильдия шахтёров`, a `Шахта` cave-selection mode for cartographer-known small caves, construction zones, worker-delivered route reinforcement at 1 wood per cell, and final placeholder mine construction after the route is prepared.
- Improved mine construction feedback: mine workers now spawn at the Miners Guild, walk through the castle to pick up wood, visibly carry it to the selected route cell, play a 2-second strengthening animation, place wall torches on reinforced cells, keep reinforced routes lit for F2/respawn blocking, and expose both mine zones and completed mines through Object MicroHUD.
- Added `Железо` as a fourth treasury resource in `ResourceWallet`/top HUD, guaranteed mixed iron/gold ore deposits when at least two deposits exist, and upgraded completed mines into ore-specific producers that store batches of 10 then deliver iron or gold to the castle by visible mine carts.
- Added iron-based building upgrades: the castle HUD now has an `Улучшения` tab for castle/farm/lumberjack/alchemist/tavern/forge levels, upgrades spend treasury iron, production/service caps react to levels, forge level gates tier-2/tier-3 gear, and completed mines can be upgraded from their MicroHUD for larger/faster resource batches.
- Rebalanced elite mob scaling: mini-bosses and bosses now use species-specific minimum HP/ATK/ARM values, scale upward on deeper dungeon levels, log their generated stats, and pay larger gold/XP rewards that match the higher fight risk.
- Fixed mine construction flow: mine workers and mine carts now prefer actual base road paths and then use only walkable labyrinth route cells, final mine construction is performed by a visible worker instead of a timer-only completion, mine torch light contributes to F2 visibility/respawn blocking, and torches are spaced by their real light coverage instead of being placed on every fortified cell.
- Restricted iron spending to building/mine upgrades only: forge gear purchases once again cost personal hero gold only, while the forge level gates available gear tiers. Added project texture assets for reinforced wooden floors/walls and applied them to mine route fortifications, including adjacent wall faces.

## 2026-05-26

- Moderately strengthened Chapel blessings: Golden Vow now grants +35% mob gold, Pilgrim Light gives +2 sight, True Hand gives +5 first-hit attack, Strong Bones gives +4 HP, Hearty Path gives +7 stamina on the first ration, Dark Hunter gives +8 XP, Vow of Return uses a 45% return step interval, Cartographer Mercy reveals the 8-neighbor ring around personal memory, and Silent Step blocks dark respawn around remembered paths with a 2-cell radius.
- Expanded mine diagnostics in `debug.log`: mine workers and mine carts now get traceable IDs, event logs for spawn/assignment/build/return/dismiss/delivery, waypoint counts, target cells, approximate grid/world positions, and throttled runtime traces while they are active.
- Fixed a mine-worker update crash caused by completing a mine and dismissing all workers while iterating the active worker list; the worker update loop now clamps its index after removals, and mine world-position logs use invariant decimal formatting for clearer diagnostics.
- Analyzed mine construction logs and parallelized route reinforcement: waiting miners can now take different unfortified route cells at the same time, assigned cells are reserved to prevent duplicates, and guild-to-castle road logs now include an explicit road path preview to verify that miners leave the guild along completed roads before entering the dungeon.
- Unified runtime HUD closing: Escape now closes any open in-game HUD before falling back to the pause/main menu, and primary clicks outside all HUD rectangles close open hero/base/building/mob/object/victory/expanded-map panels without passing the click through to the world.
- Added fortified-cell hero movement bonus: heroes now move 20% faster while standing on either manually fortified dungeon cells or mine-route reinforced cells, and the fortified speed multiplier is applied after other step-interval modifiers such as Vow of Return.
- Tightened mine caravan routing: mine carts now dispatch only after validating the whole mine route as walkable, adjacent, and fortified, then continue from the labyrinth entrance to the castle only over a completed road; the old outside-cell fallback path was removed.
- Expanded non-spam hero/mob diagnostics: heroes now log state transitions, entrance restores, return-path failures, exploration milestones, and throttled progress traces; mobs now log elite spawns/patrol traces, encounters, removals, dark-respawn summaries, and combat turns log attack/armor/damage/HP/reward details.
- Rebalanced regular mob progression without hero-level stat scaling: dark respawn now chooses Rat/Goblin/Orc weights from an overall party threat stage, phases rats out entirely at higher hero progress, keeps rats limited to the entrance-side section, removes rat bosses/mini-bosses, globally strengthens mob base stats, and shifts rewards so rats pay less while goblins/orcs/elites pay more.
- Tightened mine worker/cart origins and route validation: mine workers now instantiate from the Miners Guild anchor even if a road path is rebuilt, route-cell assignments require the whole worker prefix to be fortified or reserved by other active miners, and mine carts explicitly start at the mine cave center while logging the route in mine-to-entrance order.
- Added the Market as a buildable city service: it costs 70 gold/20 wood, renders as a clickable trade stall with roads/visibility/city ambience/tax-footprint support, appears in the castle building tab, and its MicroHUD can buy or sell food, wood, and iron in batches with prices that rise when the treasury stock of that resource is low.
- Added a separate terrain decoration layer for the outside world: seeded rivers, bridges on road crossings, low visual hills, trees, bushes, rocks, and flower patches now render outside the labyrinth without colliders, scale by map size, avoid maze/building zones, clear around newly built structures, and log generation counts for debugging.
- Reworked outside terrain visuals: the primary river now detours around the castle-side area from below, river corner cells render with rounded turn pieces, road crossings create raised bridges, and the old standalone hill primitives were replaced by seeded uneven meadow geometry in the terrain visual mesh.
- Blocked city building placement on river cells and within one cell of river banks by wiring terrain-decoration river occupancy into `BaseDevelopment` placement checks before a building model is created.
- Fixed river corner orientation: turn cells now draw one arm toward the previous river cell and the other toward the next cell, instead of mirroring the incoming direction.
- Added a generated stylized river-water tile texture under `Assets/Resources/Textures/Terrain` and connected terrain river materials to load it at runtime with a blue fallback if the asset is unavailable.
- Replaced river rendering with dedicated textured tile variants: straight horizontal/vertical river tiles and four smooth corner tiles now include water, shallow edges, banks, and grass margins in the texture itself.
- Added subtle animated river flow overlays: straight and corner river cells now place translucent ripple quads above the water area, and a shared runtime material scrolls its UVs with game time while banks/grass remain static.
- Fixed river-flow overlays rendering as static white rectangles by switching the flow material to a transparent-capable shader path, disabling mipmaps on the procedural alpha texture, and skipping overlay creation if no transparent shader is available.
- Moved generated rivers one full cell farther from the labyrinth footprint: primary and secondary river routes now use the outer offset line, and river placement rejects cells directly adjacent to maze cells.
- Made city ambience walkers treat rivers as blocked terrain unless a bridge exists on that river cell, so misc townspeople route across completed bridge crossings instead of walking through water.

## 2026-05-27

- Added the Antiquary as a buildable city service: it costs 80 gold/25 wood, renders as a clickable artifact shop with roads, visibility, city ambience, tax-footprint avoidance, castle HUD support, and a MicroHUD service entry.
- Added the `Камень возвращения` artifact for 100 personal hero gold: knights buy it at the labyrinth entrance when the Antiquary exists and their artifact slot is free, then consume it to teleport back to the entrance when returning or stuck outside the entrance.
- Softened the early regular-mob curve: rats/goblins/orcs now have lower non-elite base stats, near-entrance regular spawns bias harder toward goblins over orcs, and dark respawn stays in rat/goblin-heavy stages for a little longer before orcs dominate.
- Added fallen-knight memorial tokens: defeated knights now leave a pickup token in the labyrinth, houses stay in the city with a memorial status, other knights can carry the token in the empty inventory slot back to the entrance for +10 XP, and returned tokens attach visually to the fallen knight's house.

## 2026-05-28

- Changed hero creation after deaths into slot rebirth: hero houses now remain occupied slots, dead hero numbers are remembered, the castle action resurrects a defeated hero with the same number before building any new house, and a full set of houses blocks hiring beyond the castle hero cap.
- Added a 20-name knight title catalog in a deliberately overblown chivalric style; new and reborn heroes now receive random `Сэр ...` names, hero houses show the assigned name, and the hero HUD uses the name in both the full panel and compact hero list.
- Added hero lineage continuity: each hero-house slot now keeps one base knight name, successors spawn as the same name with an ordinal suffix (`Второй`, `Третий`, etc.), inherit 25% of the fallen hero's XP as legacy experience, and keep ancestor death tokens instead of clearing them on rebirth. Death tokens now store the exact fallen hero name, have unique inventory IDs, and multiple returned plaques can attach to the same hero house.
- Added a hero-house genealogy HUD: every `Дом героя` MicroHUD now has a `Родословная` button that opens a separate scrollable lineage window with the house summary, all generations, current successor stats, fallen ancestors, death positions, and returned/unreturned token state. Returned death tokens now notify the lineage system so the tree updates when a memorial token reaches the entrance; shared runtime HUD close helpers were split into `GameBootstrap.Hud.cs` to keep the main bootstrap under the file-size limit.
- Added hero-house funds: hero personal gold now tracks earnings since the last entrance return, and on each entrance restore the hero reserves a 20% contribution if the trip earned at least 5 gold. A small fund courier waits for completed roads, walks from the labyrinth entrance through the castle road to the current hero house, and only then deposits the gold into the lineage fund; genealogy and house MicroHUD text show fund and contribution totals.
- Softened hero-house fund contributions and tied them to per-family successor strength: heroes now donate 10% of entrance-trip earnings only when at least 10 gold was earned, keep a 20-gold personal reserve, and cap each return contribution at 10 gold. Each family house calculates a `Выучка` score from fallen ancestors, returned death tokens, and a 100-gold family fund; the resulting modest HP/stamina/attack bonus is applied only to newly created or reborn heirs and is shown in hero, house, and genealogy HUDs.
- Added narrative vengeance quests for heirs: each fallen knight now stores a death context, and the next son receives a personal `Клятва мести` based on that context. Quests cover rats, goblins, orcs, mini-bosses, bosses, carried death tokens, lost gold ingots, dark deaths, closed doors/stairs, and lower-level deaths; completing a quest unlocks a modest thematic perk, updates hero/house/genealogy HUD text, and logs quest progress.
- Refactored oversized runtime scripts below the 1000-line project limit by splitting hero exploration pathfinding, base ambience runtime state, terrain-decoration materials/runtime state, mine cave selection, and maze cave placement into partial files. Fixed death-context capture so carried ingots/tokens are recorded before death drops, and destroyed stale gold-ingot visuals when an ingot is dropped and re-rendered.
- Fixed hero HUD text fitting for long knight names, vengeance/blessing cards, and inventory slots by adding dynamic font shrinking plus ellipsis fallback; compact hero cards now use a two-line shortened knight name instead of overflowing the card.
- Expanded hero HUD affordances with hover explanations for status, HP/stamina, gold/level/training, blessings, vengeance quests, combat stats, and every inventory slot; added compact drawn icons for those panels plus fallback item-type icons for weapons, armor, artifacts, consumables, footwear, and empty slots.
- Raised combat difficulty with a Hardcore v1 balance pass: regular rats/goblins/orcs and elite mob stat blocks now have higher HP/attack/armor so more enemies survive the hero's opening hit, while combat XP/gold rewards were increased more modestly and elite rewards now vary by mob species.
- Added deterministic stat ranges for combat: mobs now roll HP/attack/armor from species/rank ranges at spawn, heroes roll personal base HP/stamina/attack/armor from their map/house/generation seed, and combat XP rewards now roll within species/rank reward ranges instead of using fixed XP values.
- Reworked combat from alternating raw hits into action-based rounds: heroes and mobs now roll initiative, spend combat stamina, choose light/heavy/guarded/recover/feint/armor-break/desperate actions, use guard and temporary armor break, suffer logged combat wounds, trigger elite phases, and show floating action/state feedback. Infirmary service now also heals combat wounds for food, and combat debug logs include round plans, damage math, HP before/after, guard/armor/wound changes, and summary totals.
- Tightened maze generation around route variety: the generator now validates important route goals for alternative paths, carves extra same-section connections near failed main routes, allows safe two-contact caves, and logs alternative-route pass/fail/carve counts. Custom map input now uses width 21-101 and height 15-101 with separate validation in the start menu.
- Fixed over-strict alternative-route validation: important maze goals now require at least one real alternate bypass on the route instead of requiring every non-endpoint route cell to be individually bypassable, preventing valid looped maps from failing generation as if they had no alternate path.
- Reworked route-variety validation toward early player choice: important maze goals now require a meaningful alternate route that opens within the early segment from the entrance/section start, and the branch carver prioritizes early same-section connectors before accepting broader route loops.
- Fixed follow-up diagnostics/runtime issues from the latest debug log: dark respawn now waits while defeated mobs are pending removal, respawn blocking is based on stable safety/torch/Silent Step cells instead of full F2 lighting, mine worker build timers clamp at zero with invariant formatting, and gold ingot/death token pickup-delivery logs include the carrying hero and inventory slot.
- Slowed moment-to-moment readability: default knight movement step interval is slightly longer, and combat rounds now resolve the first and reply actions with a dedicated pause between them plus longer opening/round/final delays.
- Further improved combat readability: combat floating text now uses a smaller dedicated style, secondary effects are delayed and merged into one compact line, combat state popups are delayed, and battle action/round/finish pauses are longer.
- Added a general same-cell stagger queue for non-combat floating text, so pickups, deliveries, rewards, healing, purchases, and production feedback no longer spawn multiple labels on top of each other in the same moment; default world floating text is also slightly smaller and rises more calmly.
- Slightly softened only the first level's initial regular mob spawn: opening rats/goblins/orcs now use lower stat ranges, and the initial spread forces orcs less aggressively while keeping dark respawn, elites, bosses, and deeper levels at the existing hardcore values.
- Fixed carried key loss on knight death: central-room and descent keys now exist as reusable world pickups, drop from the carry slot after death, render with object hover info, and can be picked up again by another knight; key pickups now take priority over other same-cell carry objectives.

## 2026-05-29

- Added personal combat trauma progression for knights: light wounds can escalate into a single heavy injury, untreated heavy injuries can leave one permanent personal scar, and successor character traits are generated separately from death context without inheriting scars.
- Updated healing rules and feedback: potions heal HP plus light wounds, the infirmary heals HP/light wounds/heavy injuries for food, rations restore only stamina, and scars are never healed. Combat, healing, death, rebirth, and reward logs now include wound/injury/scar/trait state.
- Extended hero and lineage HUDs with compact injury/scar/character cards and hover explanations so the new combat consequences are visible without overflowing the existing panel.
- Tightened the hero HUD after the trauma pass: wound/injury/scar/character cells are now text-first instead of icon-first, empty inventory slots no longer draw item-type icons, and the inventory rows are slightly denser so the close button no longer overlaps the last slot.
- Made the hero HUD blessing and vengeance card titles render with a fixed high-contrast label instead of auto-fit text, preventing those two card names from disappearing at compact panel widths.
- Consolidated the hero HUD into semantic blocks: level moved into the header XP bar, wounds/injury/scar moved under state, blessing/vengeance/character merged into a single legacy block, attack/armor merged into one combat row, and the inventory scroll area now sizes from the close button gap.
- Enlarged and tightened the hero HUD so inventory fits without a lower scroll view, and added colon separators to compact HUD parameter labels for clearer `label: value` reading.
- Added a first voxel-style visual pass: shared `VoxelVisuals` now converts round character/prop primitives into block forms, applies flatter lit materials and subtle top caps to readable blocks, voxelizes actors/buildings/items/terrain decorations/worker ambience, quantizes meadow height/color sampling, and slightly retunes the main directional light for clearer chunky silhouettes.
- Fixed early alternative-route generation stability: `MazeBranchCarver` now revisits all route goals across several stabilization passes because later key/stairs detour carving can change the shortest primary route and invalidate an earlier central-room entrance branch.
- Upgraded the labyrinth itself from voxel-like primitives to per-cell voxel mesh grids: walls now render as stacked 3x4x3 block meshes with cracked/moss stone palette variation, floors/entrances render as 3x3 slab grids with tile color variation, and per-cell renderers are preserved for visibility/F2 lighting.
- Pushed voxel visuals into buildings and interactions: large primitive building/prop cubes now hide their shell renderer and render child voxel block-grid meshes with per-material light/dark variants, while combat damage and positive pickup/reward feedback spawn animated voxel bursts tied to the same grid positions as floating text.
- Extended voxel block-grid rendering to small figure parts: knight bodies, helmets, limbs, shields, swords, mob parts, workers, couriers, tax collectors, city walkers, and role props now use lower-threshold character voxel grids instead of only simple cube shells.
- Added automatic voxel figure animation: character voxel-grid parts now receive a stepped idle animator with subtle bob/rotation/scale pulses, accessory props such as weapons, shields, tools, mugs, bottles, packs, and scrolls get a stronger discrete voxel wobble, and moving carts/carryable keys, ingots, and death tokens use the same discrete voxel motion without animating static architecture.
- Fixed a late maze-generation regression where second-half detour carving could satisfy an old farthest target while validation chose a new farthest target after carving; route goals are now rebuilt during stabilization, short-map detour span is less over-strict, and editor smoke tests cover the failed `101x25/634146015` seed plus several custom map sizes.
- Increased voxel visual density across the whole scene: generic object grids now cover much smaller opaque parts with up to 12x14x12 blocks, character/accessory parts use finer 7x9x7 grids, labyrinth walls/floors render with denser voxel-cell meshes, and the outside meadow visual mesh now uses half-cell stepped terrain with a sharper point-filtered texture.
- Made the voxel pass visibly finer and more separated: generic objects now reach 22x26x22 block grids, characters/accessories reach 14x18x14, block gaps and material contrast are stronger, labyrinth walls/floors use 8x10x8 and 12x12 grids, and the outside meadow mesh now subdivides each cell into 4x4 stepped tiles.
- Added a main-menu generation progress bar by moving start-game setup into a coroutine with staged loading statuses, and fixed the missing/corrupted terrain visual by switching the high-subdivision meadow mesh to 32-bit indices when it exceeds the 65k vertex limit.
- Smoothed long generation loads by splitting terrain meadow mesh creation, voxel labyrinth rendering, and terrain decoration spawning across multiple frames; the main-menu loading bar now animates toward progress targets while those heavy stages are actively running instead of freezing on one status.
- Added the first detailed voxel-lighting pass: voxel block grids now carry per-face vertex lighting colors through a custom voxel vertex-color shader, static maze floor/wall voxel meshes no longer cast mass realtime shadows, hero/torch point lights are cheaper non-shadow lights, key/lamp/fire/magic materials use brighter emissive profiles, and a static per-cell voxel light grid tints maze cells around entrances, doors, keys, chests, stairs, and ore.
- Retuned knight visibility lights from cool blue to warm torchlike amber so active heroes read as human warm light sources in the voxel lighting pass.
- Replaced the hard hero lighting read with a soft transparent floor-light overlay: hero light now uses subdivided vertex-alpha quads with radial falloff and low spill into adjacent walkable cells, while renderer visibility writes, fog overlay activation, and mob respawn blocked-cell rebuilding were reduced with small runtime caches/timer gates.
- Fixed meadow seam artifacts from the finer terrain pass: the visual ground mesh now maps the meadow texture once across the full generated terrain instead of repeating it at short intervals, and the runtime meadow texture uses no mip chain plus clamp wrapping to avoid bright tile/mip seams.
- Shortened map loading after the dense voxel pass: maze cell rendering now yields in much larger time-budgeted batches, terrain mesh async rows are grouped more aggressively, large-map outside decoration counts are lower, voxel block grids preallocate mesh buffers, maze-cell renderer tracking avoids duplicate child searches, and generation stages log timings to debug output.
- Added object shadow support to the voxel scene: the custom voxel shader now has URP main-light shadow receiving plus a ShadowCaster pass, meadow ground receives shadows, directional sunlight is stronger/lower with longer soft shadows, voxel shadow casting is filtered away from overlays/floors/rivers/markers, and heroes/mobs get cheap contact shadows for immediate readability.
- Made building and misc shadows visible even when realtime URP shadows are subtle or disabled: solid voxel building parts now add low-cost projected ground shadows, city walkers, tax collectors, mine workers, fund couriers, and farm carts get contact shadows, and shadow helpers were split into `VoxelVisuals.Shadows.cs` to keep the core visual file under the line limit.
- Added generated material textures across the voxel scene: `GeneratedTextureLibrary` now creates cached point-filtered procedural modulation maps for stone, moss, wood, straw, roof tile, clay, dirt, grass, metal, gold, cloth, leather, skin, parchment, glass, ore, coal, and generic solid surfaces; `VoxelVisuals.CreateLitMaterial` applies them automatically, voxel meshes now generate UVs, and `VoxelVertexColorLit` samples `_BaseMap` while preserving vertex lighting and shadow support.
- Made generated textures visually stronger after the first pass was too subtle: procedural maps now use chunkier contrast, darker seams/cracks/grain/weave, and the voxel shader samples textures in object-space by face orientation so patterns read across voxel surfaces instead of being compressed onto each tiny cube face.
- Added a visible semantic voxel-pattern fallback after generated texture maps still read as flat in-game: voxel block grids now preserve the original part name for patterning, and face vertex colors add explicit roof seams, stone mortar, wood grain, metal glints, cloth weave, leather/skin mottling, and foliage variation even if texture sampling is unavailable or too subtle.
- Made labyrinth floor and wall texture reads explicit instead of relying only on generated shader maps: maze voxel walls now use separate cracked stone, moss stone, and dark mortar materials with regular vertical/horizontal joints, while maze floors use high-contrast light/dark tile stones plus dark mortar strips so the Maze surface pattern is visible even if procedural `_BaseMap` sampling is hard to notice.
- Extended the explicit voxel texture pass beyond the Maze: automatic voxelized parts now use semantic five-material palettes instead of only base/light/dark, giving roofs seams, wood dark grain, stone mortar, metal glints, gold sparkles, cloth weave, leather/skin mottling, foliage variation, glass highlights, ore veins, dirt speckles, and parchment lines across buildings, heroes, mobs, city walkers, carts, decorations, and carryable items.
- Rebalanced the texture pass after it made scenes too dark and slower: automatic voxel grids now return to one material/submesh and use lighter vertex-color patterning plus generated texture modulation, maze walls/floors were reduced to two materials per cell, dark pattern multipliers were raised, and procedural texture modulation no longer clamps into near-black values.
- Reduced the follow-up voxel performance cost and over-darkening: automatic voxel grids now use lower caps, skip hidden interior blocks, avoid realtime shadow receiving/casting in favor of projected/contact shadows, maze wall/floor voxel density is lower, static maze ambient tint is brighter, terrain mesh subdivision is lighter, and outside decoration counts are capped more conservatively.
- Added cartoon movement animation for ambience people: city walkers, tax collectors, hero-house fund couriers, and mine workers now get small voxel feet plus a shared walk animator that bobs/sways body, head, feet, tools, and carried items only while the root is moving. Farm cart wheels now roll around the cylinder axle instead of the wrong local axis.
- Improved main actor movement readability: knights now walk with heavier body sway, armor squash/stretch, boot lift, head counter-motion, and cape drag; humanoid mobs stomp with torso/head/arm motion while rats use a faster low scurry with four-leg and tail movement. Farm carts now shake their visual body and cargo while moving without disturbing route following.
- Fixed a mob movement animation scale regression: mob body squash/stretch now multiplies the original small body scale instead of replacing it with near-unit scale, preventing moving mobs from ballooning into oversized blocky squares.
- Softened the first-level opening curve: rookie regular rats/goblins/orcs now roll much lower HP/attack/armor, level-1 spawn selection avoids early orcs near the entrance side, the entrance mob buffer expands to 8 cells, the initial regular target multiplier is lower, first-wave regular mobs wait before wandering, and dark respawn has a short opening grace period.
- Added an opening-combat safety guard after a log review showed a near-dead rookie goblin killing a fresh knight with DesperateStrike: early level-1 regular mobs no longer use desperate one-shot attacks against level-1/2 heroes in the first 40 expedition steps, and rookie goblins no longer randomly break low starter armor.
- Reworked knight lighting readability: hero light overlays now use a finer cached soft torch mesh with warm floor falloff, limited corner spill, wall-top glow, and shader-side flicker instead of rebuilding blocky yellow cells every frame. Lighting-mode Maze/fog palettes were brightened and separated, the selected-hero marker was toned down, and knights now carry a small emissive lantern on the model.
- Finished the second knight-lighting pass: Maze voxel renderers now receive cached dynamic warm tint from all visible knights, including adjacent walls and a small corner spill, then restore their static tint when leaving lighting mode. The soft overlay was widened and lowered in alpha so the actual wall/floor materials carry readability instead of a square yellow plate.
- Made market trades purely instant resource/accounting actions with no world floating text, purchase delivery animation, or purchase sound at the market position; market debug logs now mark trades as `instant=true, worldAnimation=false`. Added mine-cart dispatch backpressure so each completed mine can have only one active delivery cart and all mines share a small global active-cart cap, preventing stalled-looking cart piles near mines from being mistaken for market deliveries.
- Added sub-cell visual movement on top of the existing grid logic: `SubCellPathBuilder` turns cell paths into offset voxel-lane waypoints with entry/exit points, while logical `GridPosition` remains authoritative for combat, fog, pickup, and pathfinding. Heroes, mobs, farm carts, mine carts, mine workers, dungeon fortification workers, city walkers, tax collectors, and hero-house fund couriers now follow those smoother intra-cell visual paths.
- Opened up the labyrinth read without touching camera behavior: hero/mob visual footprints and the selection marker are smaller, maze walls render lower, inset, and lightly chamfered while floors fill the walkable cell, knight torch overlays/tints are softer, and generation now carves extra small spacious pockets in each maze section.
